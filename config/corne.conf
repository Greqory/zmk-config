CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# Wired split over TRRS (full-duplex UART)
CONFIG_ZMK_SPLIT_WIRED=y
CONFIG_ZMK_SPLIT_WIRED_UART_MODE_INTERRUPT=y

# Disable Bluetooth (USB + wired only)
CONFIG_ZMK_BLE=n